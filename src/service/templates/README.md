# Templates

Phase 6 template schema, parser, and builtin template registry live here.

The current foundation intentionally ships JSON-first and keeps import/export, persistence, and template marketplace work for later tasks.
