# Screenshot Helper

Planned native helper boundary for on-demand screenshots.

Current scaffold covers:

- `Ctrl+Shift+S` shortcut reservation
- screenshot artifact handoff contract
- manual submit path into the `image` pipeline

Real overlay capture is deferred to a later native implementation step.
