# 未完成任务与状态追踪

**用途**：记录目前仍待做的模块化/功能升级项，以及每次迭代实际落地的动作。最新状态在顶部，已完成的条目保留做历史审计。

---

## 状态图例

- 🔴 未开始（BLOCKER / P0）
- 🟡 部分完成 / 进行中
- 🟢 已完成
- ⚪ 暂缓 / 非本轮优先级

---

## Part A — 待办清单（按优先级）

### P0 — 阻塞已声称的能力 / 框架升级

| ID | 项目 | 状态 | 说明 |
|---|---|---|---|
| **P0-0** | **任务理解层（TaskPlan）Week 1** | 🟢 | 2026-04-20：触发器 + 计划执行器（仅时间偏移分支）已落地。[src/service/core/intent/trigger.mjs](src/service/core/intent/trigger.mjs) 纯代码检测 "5 分钟后 / 明天 X 点" 等一次性偏移；[src/service/core/intent/plan-executor.mjs](src/service/core/intent/plan-executor.mjs) 直接创建 one-shot schedule 并返回 "已安排" 气泡。**零 LLM 调用**，比旧路径更快（旧路径会跑 4 次 web_search 才发现要调度）。`agent-loop.mjs` 的 rule #13 已移除。Week 2-4 见下。 |
| P0-0b | TaskPlan Week 2：数量词 / 指代 | 🔴 | 触发器加 `/(所有\|全部\|every\|each\|all)\s*(的\|files?\|items?)/` 和 `/(这个\|那个\|this\|that)\s*(文件\|图片\|邮件)/` 且 context_packet 无对应资源。Week 2 引入第一次可选 LLM 调用（单次，只用于分解 fan-out 步骤），失败降级到 fast path。 |
| P0-0c | TaskPlan Week 3：clarifications_needed | 🔴 | 让 plan 输出 `clarifications_needed[]`，聊天气泡里直接以 mini-form 收集缺失字段（收件人、主题等），回答后 re-invoke 理解器而不是开新 task。依赖 Week 2 的 LLM 理解器。 |
| P0-0d | TaskPlan Week 4：删除已被取代的补丁 | 🔴 | 落地后删除：agent-loop.mjs 的 rule #4（MISSING INFORMATION）、intent-router 的 RULE 表里意图判断（只保留 fast-path 触发）、image-submission 的 `looksLikeConnectorFileSend`、connector-intent 的 `matchWorkflowByTrigger`。每删一项加一条回归测试。 |
| P0-1 | Microsoft 发信 scope 补齐 | 🔴 | `MICROSOFT_SCOPES` 还只是读权限，缺 `Mail.Send` / `Calendars.ReadWrite` / `Files.ReadWrite`。Outlook workflow 契约齐了但真跑发不出去。 |
| P0-2 | 外部 MCP 工具接入 catalog | 🔴 | `mcp-catalog-bridge.mjs` 写了但 `service-bootstrap.mjs` 没调用。外部 MCP 工具仍通过旧 `client-bridge.getMcpActionTools()` 旁路直接注入 agentic prompt，绕过 catalog 的 risk / confirmation / timeline 策略。Phase F 宣称做完但实际没通。 |
| ~~P0-3~~ | ~~调度器支持一次性触发~~ | 🟢 | 2026-04-20：`nl_to_cron.mjs` 加一次性模式 + schema 暴露 `type="at"`。**已被 P0-0 取代**：rule #13 删除，触发逻辑改由 [plan-executor.mjs](src/service/core/intent/plan-executor.mjs) 在 submission 入口直接处理。NL 解析器 + `at` trigger type 保留（plan-executor 内部用）。 |

### P1 — 模块化债务

| ID | 项目 | 状态 | 说明 |
|---|---|---|---|
| P1-1 | 继续拆 `http-server.mjs` | 🔴 | 当前 2877 行，只抽走了连接器路由。剩余：task 提交、skills、AI provider config、MCP config、history、templates、DAG、budget、schedules、audit、office、browser extension。按 `connector-routes.mjs` 的模板拆 4–5 个路由文件。 |
| P1-2 | 拆 `agent-loop.mjs` | 🔴 | 1085 行。建议拆 `planners/llm.mjs` / `planners/default.mjs` / `guards/truthfulness.mjs` / `guards/refusal.mjs` / `conversation-messages.mjs` / `intent-heuristics.mjs`。 |
| P1-3 | `account-connectors.mjs` 拆 provider | 🔴 | OAuth flow 混了 Google/Microsoft 共用 + provider-specific URL。理论应拆 `core/oauth-flow.mjs` + `google/oauth.mjs` + `microsoft/oauth.mjs`，与连接器契约目录对齐。 |

### P2 — 产品完整性

| ID | 项目 | 状态 | 说明 |
|---|---|---|---|
| P2-1 | Console Plugin 管理 UI | 🔴 | HTTP `/plugins` 端点和 `connector_plugin_manage` action tool 都在，但桌面 Console 侧边栏没加 "Plugins" 页。用户看不到已装插件、无法点 install/uninstall/enable。 |
| P2-2 | Plugin 安装支持 zip / URL | 🔴 | `plugin-registry.install()` 只接受本地目录。PLUGIN_LIFECYCLE.md 里写的 "sourceUrl + sha256" 还没实现。 |
| P2-3 | Plugin uninstall 反注册 mcpServers | 🔴 | install 时会把 `manifest.mcpServers` 注册进 `runtime.mcpRegistry`，但 `uninstall()` 只删目录 + reload catalog，registry 里的 entry 没移除。 |
| P2-4 | Microsoft 工作流端到端实测 | 🔴 | Outlook draft-confirm-send、日历创建、OneDrive 上传下载都是契约齐了、没真跑过。依赖 P0-1 先补写权限。 |
| P2-5 | Gmail 真实 `drafts.create` | 🔴 | 当前 `create_draft_preview` 只本地拼文本。Gmail Web "已保存草稿" 列表里看不到。改用 `https://gmail.googleapis.com/gmail/v1/users/me/drafts` 真建 draft，再在 send 时调 `drafts.send`。 |
| P2-6 | 附件 pre-flight size 校验 | 🔴 | Gmail 单封 25MB 限制，OneDrive 150MB，目前超限直接 Gmail API 413，错误不友好。调度前应先 stat 附件文件、合计 base64 膨胀率后比较上限。 |
| P2-7 | Drive download 支持指定导出格式 | 🔴 | 当前 Google Docs/Sheets/Slides 固定导出 docx/xlsx/pptx。用户想要 PDF/ODT 需要命令参数 `exportMimeType`。 |

### P3 — 观测性 / 稳定性 / 测试

| ID | 项目 | 状态 | 说明 |
|---|---|---|---|
| P3-1 | KNOWN_FAILURES 里 2 个 brand 断言修正 | 🔴 | `verify-office-base.mjs` / `verify-release-readiness.mjs` 还断言 "UCA for Word"。非 connector 相关，拖累 `npm run check` 全绿。 |
| P3-2 | 审批卡跨对话 SSE 去重 | 🔴 | `renderedApprovalCards` 是模块级 Map，切对话后回来可能误过滤。应按 `conversationId` 隔离。 |
| P3-3 | Plugin manifest JSON schema 严格校验 | 🔴 | 目前只 try/parse + 检查 `id`。应加 Ajv/JSON-Schema 校验，防止恶意 plugin 注入奇形 contracts。 |
| P3-4 | `buildRfc822EmailAsync` 单元测试 | 🔴 | MIME 拼装易错，attachment 路径没覆盖。 |
| P3-5 | `edit-before-confirm` overrides merge 路径测试 | 🔴 | scheduler/engine.mjs 的 overrides 注入点没有 verify 覆盖。 |
| P3-6 | Timeline 事件的 artifact 预览链接 | 🔴 | 目前 timeline 只有 summary，点不开完整内容。draft preview / search result 应支持"展开查看"。 |

---

## Part B — 本轮迭代执行记录

### 2026-04-20（下午）—— P0-0 TaskPlan Week 1 落地

**架构层变化**：把时间偏移任务调度从"LLM 靠系统提示自觉"升级为"框架层纯代码分流"。

- 🟢 新建 [src/service/core/intent/trigger.mjs](src/service/core/intent/trigger.mjs)：`detectTimeOffset()` 纯正则，0 LLM；`shouldRunTaskPlan()` 作为公共门。
- 🟢 新建 [src/service/core/intent/plan-executor.mjs](src/service/core/intent/plan-executor.mjs)：`maybeHandleAsPlan()` 时间偏移命中 → 调 `runtime.scheduler.createSchedule({type:"at"})` → 生成 "已安排 X 分钟后执行" 气泡 → END。
- 🟢 [context-submission.mjs](src/service/core/context-submission.mjs) 和 [image-submission.mjs](src/service/core/image-submission.mjs)（后者间接通过 context-submission）在 `routeIntent` 之前先调 plan 层；命中即 return，不命中走原路径。
- 🟢 scheduler 触发时 `submitContextTask({skipPlanLayer:true})` 避免调度任务再被调度（无限循环）。
- 🟢 [agent-loop.mjs](src/service/executors/tool_using/agent-loop.mjs) **删除 rule #13**（系统提示层"schedule first"指令），改为 rule #13 "TIME OFFSETS"：告知 LLM 上游 plan 层已经处理过、你看到的 userCommand 是残余指令。
- 🟢 附件/图片在 plan-executor 里被存到 `schedule.action_params.{file_paths,image_paths}`，并在 scheduler 触发时由 [execute-action.mjs](src/service/scheduler/execute-action.mjs) 带回 `buildSchedulerContextPacket`，保证定时任务触发后仍能看到原附件。
- 🟢 新增 [scripts/verify-intent-plan.mjs](scripts/verify-intent-plan.mjs) 覆盖：
  - 相对偏移 / 绝对偏移 / 英文短语都命中
  - 循环模式（"每 5 分钟" / daily / weekly）正确跳过
  - 普通命令原样返回 null
  - plan-executor 创建的 schedule 里 `action.params.userCommand` 含残余指令
  - 原命令的附件路径被带进 schedule.action_params
  - 仅输入偏移短语（无后续动作）时 fall-through，由 LLM 向用户澄清
- 🟢 `npm run check` 串入 `verify:intent-plan`。

**性能**：零新增 LLM 调用。触发器是 1 条正则 + 1 条排除正则，在 submission 入口 microseconds。不触发的请求（简单"打开 outlook"等）走原路径无差异。触发的请求**反而更快** —— 原先 LLM 会跑 4 次 web_search + 2 次失败的 schedule 调用，现在直接 schedule 完返回。

### 2026-04-20（上午/中午）—— 审批卡 + 编辑 + 可下载 + 一次性调度

**新增能力**
- 🟢 审批卡片 subject/body/to 可编辑，确认时携带 `overrides` POST；后端 `scheduler/engine.mjs` merge overrides 到 workflow input
- 🟢 附件发信：Gmail 多段 MIME (base64)，Outlook Graph `fileAttachment`
- 🟢 Drive/OneDrive 下载工具 (`account_download_file` + `google.drive.download_file` / `microsoft.onedrive.download_file` 契约)
- 🟢 "把文件发给 X" 路由修正：image-submission 检测 connector-send 意图时改走 tool_using，附件路径注入 `context_packet.file_paths`
- 🟢 系统提示增加"附件 available"段
- 🟢 **一次性定时任务支持 (P0-3)**：NL 解析 "N 分钟/小时后"、"明天/今晚 X 点"；schema 暴露 `type=at`；LLM 系统提示 rule #13 教导 schedule-first 模式
- 🟢 Truthfulness guard：LLM 说"已发送/sent" 但无真实 tool success 时降级为 partial_success + 红字警告
- 🟢 Skill pattern tracker：跳过 connector workflow 内部工具 + 要求 2 个不同工具才记录
- 🟢 Email scope 补齐：Google 加 `gmail.send` / `drive.file` / `calendar.events`
- 🟢 RFC 2047 编码词：非 ASCII 主题不再乱码
- 🟢 Connector plugin manifest + install/uninstall/enable/disable HTTP + connector_plugin_manage 工具
- 🟢 Internal MCP server (`scripts/start-lingxy-mcp-server.mjs`) 暴露 catalog 给外部 MCP 客户端
- 🟢 Microsoft Outlook/Calendar/OneDrive 契约 + workflows
- 🟢 workflow dispatcher 扩展 `execution.kind === external_mcp` 分支

**修过的 bug**
- 🟢 `account_send_email` 对 `to` 是字符串时崩溃 → 工具入口归一化 + provider 函数 `asList()` 兜底
- 🟢 LLM planner 被 `planDeterministicToolCall` 的连接器兜底短路，没走 workflow → 移除 domain 兜底，让 catalog 驱动
- 🟢 `tool_using` 执行器强制 `llmPlanner` 导致 workflow-first 不生效 → 改为系统提示里注入 workflow 目录（`formatWorkflowsForPlanner`）
- 🟢 Connector HTTP 路由从 `http-server.mjs` 拆到 `http-routes/connector-routes.mjs`

**删除**
- 🟢 两个垃圾 auto-skill 目录：`google.gmail.create-draft-preview-connector-workflow-run`、`web-search-fetch-web-search-fetch`
- 🟢 `tryDispatchWorkflow` + `draftWorkflowInputFromIntent` 两个 regex-short-circuit helper（被 workflowHint 里的 LLM 自主选 workflow 取代）

### 之前几轮

- 2026-04-19：连接器框架 Phase A–G（catalog / dispatcher / workflow-submission / contract-loader / validators / plugin-registry / mcp-catalog-bridge）、6 个 verify:connector-* / verify:plugin-registry / verify:internal-mcp-server 脚本落地、docs/task-runtime 拆成 ARCHITECTURE / MCP_INTEGRATION / PLUGIN_LIFECYCLE / KNOWN_FAILURES
- 早期：UCA-077/078/079/080 连接器基础 + 账户路由 + 多账户 API + 写工具

---

## Part C — P0-0 任务理解层设计草案

> 这是 P0-0 的实现提纲，正式落地前请在此修订。

### 动机

当前问题（真实发生的 task id）：

| task_id | 命令 | 失败原因 |
|---|---|---|
| `task_12d08bd2` | 五分钟以后给我发一份美股最新的消息汇总 | LLM 立刻搜 4 次网+抓 3 个 URL（现在数据），再 create_scheduled_task 不带 trigger 失败 2 次。不理解"时间偏移" |
| `task_cca19b54` | 帮我把桌面上所有的图片文件都打开 | 命中 `/(图片)/i` 路由到 multi_modal，但 context_packet 没图片 → 空跑 + 去重误杀 |
| `task_7fbd4884` | 把这个文件发给 user-b@example.com | 有图片附件 → 无条件路由 multi_modal（无 connector 工具）→ LLM 说"I don't have email tools" |
| `task_b2f447e2` | 给 user-a@example.com 发一份邮件，向他问好。用 gmail | LLM 连问 3 轮文字"主题写什么"最终编造"已发送"（也是内容分解+澄清缺失） |

共同症状：框架**假设一条文本 = 一个意图 = 一个执行器**，用关键字绕过理解。

### 设计

```
用户输入 → 触发器 → [理解器 LLM, 可选] → TaskPlan → 执行器
             ↑
         纯代码，0 LLM
```

#### 触发器 (`src/service/core/intent/trigger.mjs`)

纯代码判断是否需要理解。触发条件（任一命中即进入理解器）：

1. **时间偏移**：正则匹配 "(N 分钟/小时/天|明天|今晚|之后|after|later|in N)" 且不是循环 ("每天/每周/every")
2. **数量词**：`/(所有|全部|every|each|all)\s*(的|files?|items?)/`
3. **多动词连接**：`/(然后|接着|之后|and\s+then|再)/`
4. **指代歧义**：`/(这个|那个|this|that)\s*(文件|图片|邮件|file|image|email)/` 且 context_packet 无对应资源
5. **显式问句**：命令以 `？|?` 结尾且非单问句
6. **命令中提到 "发送/上传/创建" + 目标域，但缺关键参数**（收件人、主题、路径）

不触发则跳过理解器，走当前 fast / context / agentic 路径（保留现有速度）。

#### 理解器 (`src/service/core/intent/understand.mjs`)

一次 LLM 调用，system prompt 简短，产出严格 JSON：

```json
{
  "summary": "用户想 5 分钟后收到一份美股简报邮件",
  "steps": [
    {
      "id": "s1",
      "intent": "send_email",
      "when": "at:+300s",                       // now | at:<iso> | at:+<seconds>
      "executor_hint": "tool_using",
      "inputs": {
        "to": ["user_inferred_from_context"],
        "subject": "",
        "body": "",
        "needs_data": ["us_stock_latest_news"]  // 决定了 exec 时先 search
      },
      "depends_on": []
    }
  ],
  "clarifications_needed": [
    {
      "question": "收件人邮箱是？",
      "applies_to_step": "s1",
      "inputs_field": "to",
      "blocking": true                          // true = 不回答不能执行
    }
  ],
  "confidence": "high"                          // high / low，low 时走原路径
}
```

#### 执行器 (`src/service/core/intent/plan-executor.mjs`)

- 有 blocking clarification → 用当前聊天卡 UI 问用户，等回答 → re-invoke 理解器（带用户回答）
- 每个 step 按 `depends_on` 拓扑排序
- `step.when === "now"` → 立即按 `executor_hint` 调度
- `step.when === "at:<iso>"` → 调 `create_scheduled_task` 建 one-shot（直接走后端，不让 LLM 再决策）
- `step.inputs.needs_data` → 在执行器里先运行对应 data-fetch，再填入

#### 可删除的补丁

P0-0 落地后：
- 🗑️ [agent-loop.mjs rule #4 "MISSING INFORMATION"](src/service/executors/tool_using/agent-loop.mjs)：由 clarifications_needed 接管
- 🗑️ rule #13 "SCHEDULED TASKS"：由 step.when 接管
- 🗑️ [image-submission.mjs `looksLikeConnectorFileSend`](src/service/core/image-submission.mjs)：由 step.intent + attachments 接管
- 🗑️ [connector-intent.mjs `matchWorkflowByTrigger`](src/service/connectors/core/connector-intent.mjs)：由 step.intent 直接映射
- 🗑️ [agent-loop.mjs truthfulness guard](src/service/executors/tool_using/agent-loop.mjs)：由 plan 的 step-success 闭环追踪接管
- 🗑️ [intent-router.mjs RULE 表里的意图判断](src/service/core/router/intent-router.mjs)：只保留 fast-path 触发，意图由 plan 给出

#### 非目标（避免重蹈 Task Runtime 覆辙）

- ❌ 不对每条文本都跑 plan（触发器把关）
- ❌ plan 不包含 verifier 循环（各执行器自己的成功判断复用）
- ❌ plan 不再加 "normalize" 和 "classify" 两个独立 LLM 调用
- ❌ plan 失败时 **降级**而不是阻塞：直接把原文本交给当前默认路径

#### 落地顺序与当前进度

1. **Week 1 🟢 已完成** (2026-04-20)：触发器 + plan-executor（仅时间偏移分支），零 LLM 调用、直接创建 one-shot schedule。Rule #13 已连带删除。
2. Week 2 🔴：加 "数量词 / 多动词 / 指代歧义" 触发。首次引入理解器 LLM 调用（单次）。失败降级到 fast path。
3. Week 3 🔴：加 `clarifications_needed` 的 UI 回流 —— 聊天气泡里收集缺失字段，回答后 re-invoke 理解器而不是开新 task。
4. Week 4 🔴：删掉 rule #4 / looksLikeConnectorFileSend / matchWorkflowByTrigger 等补丁。每删一项加一条回归测试。

---

## 更新约定

- 每次做完一批工作，把对应条目 🔴/🟡 改成 🟢 并移到 Part B 的顶部小节。
- 发现新问题直接补到 Part A 对应优先级档位。
- 单行标题 + 状态 + 简短说明。不写冗长理由（那放在对应文件 commit 描述或 GitHub issue 里）。
