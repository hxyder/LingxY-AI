"""LingxY application launcher — Python sidecar for the Node service's
``launch_app`` action_tool.

This package is invoked from Node via subprocess; the public entry point is
:func:`launcher.open_app` and the corresponding ``launcher.py`` CLI.
"""
