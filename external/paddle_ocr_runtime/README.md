# Paddle OCR Runtime

Runtime placeholder for the Phase 5 local OCR path.

Planned contents:

- portable Python runtime
- PaddleOCR model assets
- bootstrap scripts and health checks

Current scaffold records the selected engine family only:

- engine: `paddle-3.0-placeholder`
- distribution mode: on-demand install, not bundled into the main app package
