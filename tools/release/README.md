# Release Tools

This directory contains release-time configuration and packaging metadata for the trial channel.

Current scope:

- versioned trial release configuration
- bundle layout and required asset list
- inputs for `scripts/build-trial-package.mjs`
